<Application x:Class="DiplomaLol.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DiplomaLol"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="{x:Type FrameworkElement}">
            <Setter Property="Control.Foreground" Value="DarkSlateGray"/>
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderBrush" Value="Transparent"/>
            <Setter Property="Control.FontFamily" Value="Comic Sans MS"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>

        <Style TargetType="Button"  BasedOn="{StaticResource {x:Type FrameworkElement}}">
            <Setter Property="Control.FontSize" Value="30"/>
        </Style>
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type FrameworkElement}}"/>
        <Style TargetType="Separator" BasedOn="{StaticResource {x:Type FrameworkElement}}"/>

    </Application.Resources>
</Application>
